language: java

git:
  quiet: false

before_install:
- brew install gradle
- gradle installWrapper

jobs:
  allow_failures:
  - os: osx
  include:
    - jdk: openjdk11
      os: linux
    - jdk: openjdk11
      os: osx

notifications:
  email:
    recipients: 
    - steinigke300@googlemail.com

script: ./gradlew buildCore
